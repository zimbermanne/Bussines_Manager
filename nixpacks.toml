[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
# This flag overrides the 'externally-managed-environment' restriction in modern Python containers
cmds = ["pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"